/** The units the sweepers and retention windows measure in, so no module spells a day its own way. */
const HOUR_MS = 60 * 60 * 1000;
const DAY_MS = 24 * HOUR_MS;

module.exports = { HOUR_MS, DAY_MS };
